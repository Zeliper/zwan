# HANDSON — 현재 상태와 다음 작업

다음 세션은 이 파일부터 읽으면 된다.

> **이 파일은 이제 git 에 추적된다.** 기계를 갈아엎어도 남으라고 2026-08-28 에 `.gitignore` 에서 뺐다.
> 그러니 여기에 **토큰·키 지문·공인 IP 를 적지 말 것** — 공개 저장소다. 실제 값이 필요하면
> `<토큰>` 같은 자리표시자로 쓰고 값은 각자 기계에 둔다.

---

## 0. 포맷 직후 — 개발 환경 되살리기

리포에는 코드만 있다. 아래는 전부 기계에 있던 것이라 다시 깔아야 한다.

| 필요한 것 | 없으면 | 비고 |
|---|---|---|
| Go (`C:\Program Files\Go`) | 아무것도 안 됨 | `scripts/build-release.sh` 가 이 경로를 먼저 본다 |
| Node + npm | 프론트엔드 빌드 실패 | wails 가 알아서 `npm install` 한다 |
| wails v2 | GUI 빌드 실패 | `go install github.com/wailsapp/wails/v2/cmd/wails@latest` |
| NSIS (`C:\Program Files (x86)\NSIS`) | 설치본 빌드 실패 | `makensis.exe` |
| **`bin/wintun.dll`** | **어댑터 생성 실패** | **git 에 없다**(라이선스 위생 — 프리빌트 재배포물). [wintun.net](https://www.wintun.net) 에서 amd64 dll 을 받아 `bin/` 에 둔다 |
| `gh` CLI + 인증 | 릴리스 못 만듦 | `gh auth login` |
| WSL (선택) | `client/vip` 실커널 테스트만 불가 | §2.1 |

되살린 뒤 한 줄 점검:

```bash
go build ./... && go vet ./... && go test ./...
cd gui && go vet ./... && go test ./...
```

관리자 권한 통합 검증(끝나면 서버·엔진·어댑터·NRPT 규칙을 전부 정리한다):

```powershell
powershell -ExecutionPolicy Bypass -File scripts\verify-windows.ps1
```

---

## 0.1 바로 이어서 할 것

남은 일은 둘 중 하나다.

### 1. 2대 실통신의 나머지 절반

`ping` 은 됐다(§2.1). **아직 안 한 것: 이름으로 서비스 접속(TCP·UDP), MTU/큰 패킷.**
v0.4.0 에서 **앱으로 서비스를 게시**할 수 있으니 CLI 플래그 없이 된다.
**양쪽 다 0.4.0 이상**이어야 한다 — 구버전 엔진은 모르는 JSON 키(`publish`)를 조용히 버린다(§4 교훈).

```powershell
# 서비스를 내놓는 쪽: 백엔드를 하나 띄우고
powershell -File scripts\echo-probe.ps1 -Listen -Proto tcp -Port 31001
# 앱 → 가입 탭 → 네트워크 카드 펼치기 → "이 기기에서 게시" → echo / 25565 / 31001 → 저장

# 접속하는 쪽: 포트 말고 이름으로
powershell -File scripts\echo-probe.ps1 -Connect echo.<별칭> -Port 25565
powershell -File scripts\echo-probe.ps1 -Connect echo.<별칭> -Port 25565 -Size 400000
powershell -File scripts\echo-probe.ps1 -Connect voice.<별칭> -Proto udp -Port 64738 -Size 2000
```

UDP 큰 패킷이 MTU/단편화를 가장 날카롭게 본다 — TCP 처럼 조용히 쪼개지지 않는다.

**호스팅을 다시 세울 때 열어야 하는 것** (§4 에서 하루를 태운 자리):

| 어디 | 무엇 |
|---|---|
| 호스트 Windows 방화벽 | v0.3.2+ 는 서비스 설치 시 **자동으로 넣는다**. 수동이면 TCP 컨트롤 + UDP 릴레이 |
| 공유기 포워딩 | TCP 8787(가입), **UDP 3478(터널)**. 릴레이를 빼먹으면 *가입은 되는데 통신이 안 된다* |
| 호스트 "공개 주소" | 포트까지 적을 것. 안 적어도 v0.3.1+ 가 리슨 포트를 붙여 준다 |

릴레이가 밖에서 살아 있는지는 왕복으로 잴 수 있다 — `[0x02][src:4][dst:4][payload]` 를 `src == dst`
로 보내면 릴레이가 보낸 쪽을 학습한 뒤 그대로 되돌려 준다.

### 2. M3 NAT 트래버설

§3 로드맵의 다음 항목. 코드를 더 쌓는 쪽이고, §2.2-2 의 엔드포인트 구멍도 여기서 닫힌다.

---

## 1. 릴리스 이력

베이스(v0.1.1): 컨트롤 플레인, 암호 터널(직접+릴레이), Split-DNS, L4 라우터, 데스크톱 앱, NSIS 설치본, 자동 업데이트.
설계·근거는 `구현계획.md`, 원래 아이디어는 `MyWAN_가상네트워크_아이디어_정리.md` 에 있다.

| 버전 | 내용 |
|---|---|
| `v0.2.0` | 컨트롤 플레인 TLS(ACME/핀), 노드 토큰 인증, 그룹 ACL, 다중 네트워크 + 네트워크별 주소 변환, 서비스 전용 주소 + UDP, 서버도 서비스+GUI, **M2c NRPT 시스템 DNS** |
| `v0.2.1` | Host 탭 검은 화면 수정 + error boundary |
| `v0.3.0` | 한국어·영어 다국어(창 + 트레이) |
| `v0.3.1` | 공개 주소에 포트가 없으면 리슨 포트를 붙인다 |
| `v0.3.2` | 서비스 설치 시 방화벽 규칙 자동 등록, 릴레이를 리슨 주소로 광고하던 것 수정 |
| `v0.3.3` | 자동 업데이트가 호스트를 갱신하게(무인 설치가 Server 역할을 건너뛰던 것) |
| `v0.3.4` | 기기 이름 기본값(비우면 호스트명) — 없으면 이름으로 못 찾는다 |
| `v0.4.0` | **앱에서 서비스 게시**(`gui/frontend/src/PublishEditor.tsx`) |

브랜치는 `main` 하나. `feat/control-plane-tls` 는 머지 후 역할이 끝났다.

---

## 2. 검증 상태

### 2.1 검증 완료 — 다시 안 해도 된다

| 대상 | 어떻게 |
|---|---|
| **2대 실통신** | **서로 다른 공유기 뒤 2대, 릴레이 경유 `ping` 왕복 4~5ms.** 처음 증명된 것: 두 호스트 사이 실제 터널 트래픽, **Windows 에서 `client/vip` 주소 변환이 진짜 패킷을 나른 것**(그전엔 리눅스 커널 상대로만), 릴레이 데이터 경로, `/32` 라우트 |
| `client/vip` 패킷 재작성 | **실제 리눅스 커널이 판정.** WSL + root 로 진짜 TUN 을 만들어 ping/UDP/TCP-RST 왕복 |
| `client/l4` TCP·UDP 프록시 | 진짜 소켓으로 왕복·클라이언트 격리·접근거부·종료 |
| TLS/핀닝 | 실바이너리: 핀 일치 성공, 불일치·핀없음·평문 거부, 재기동 후 핀 동일 |
| 노드 토큰 인증 | 실바이너리 + PowerShell: 무인증 401, 가입토큰 재사용 401, 노드토큰 200 |
| 그룹 ACL | 실바이너리 3그룹: dev/guest 서로 안 보임, nas 만 공유, dev 전용 서비스는 guest 에게 미노출 |
| 서비스 전용 주소 | 실바이너리: 같은 포트 25565 서비스 2개가 각각 다른 주소, UDP 서비스 1개 |
| 서버 서비스 + IPC | `service run` + 파이프: status/start/stop, 재기동 시 auto-start 복귀, `autoStart:false` 영속 |
| 다중 네트워크 매니저/프로파일/IPC | 실서비스 파이프: 별칭 정규화, 실패해도 기억, 잘못된 별칭 거부, disconnect 유지 / forget 삭제, 재기동 복원 |
| NRPT 조정 로직 | 유닛: 추가·제거·재지정·중복 정리·전체 정리, 포트 53 강제, 이상한 서픽스 차단, FQDN 표기 매칭 |
| NRPT 실표 읽기 | `Get-DnsClientNrptRule` 실호출 + 파싱(권한 불필요, `go test ./client/nrpt/` 에 상시 포함) |
| **NRPT 실왕복** | 관리자 실측: 규칙 설치 → **Windows 자신의 리졸버**로 해석 → 일반 해석 무영향 → 재적용해도 규칙 1개 → 강제종료 잔재 정리 → 제거 후 이름 소멸 |
| **NRPT 2-네트워크** | `verify-windows.ps1` 전항목: 별칭마다 규칙 1개, `-Server` 없이 시스템 경로 해석, `probe.alice`=100.112.0.1 / `probe.bob`=100.113.0.1 로 분리, `localhost` 무영향, bob 만 끊으면 bob 만 소멸, 다 끊으면 0개 |
| **변환 × 서비스 주소** | 서버가 준 `100.64.128.1/.2` 를 클라가 `100.112.128.1/.2` 로 보고 **그 로컬 주소로 `/32` 라우트** |
| 다중 네트워크 실어댑터 | 어댑터 2개(MyWAN-alice/bob), 같은 오버레이(100.64.0.1)에서 로컬 주소 2개, 겹침 경고 없음 |
| 다국어 | 실행 확인: 한국어 자동 감지, 두 탭 정상, 전환 시 전체 리페인트, 모노스페이스 인용 유지. 트레이는 테스트로 |
| 방화벽 규칙 | 인자 구성(포트/프로토콜을 박지 않는지), netsh 문법(비관리자에서 파싱은 통과·권한만 거부), 관리자 게이트 왕복 |
| NSIS 설치본 빌드 | `makensis` **경고 0개**, 3섹션(1 필수) |

**`client/vip` 실커널 테스트 재실행** (WSL, root):

```bash
GOOS=linux GOARCH=amd64 go test -c -o /tmp/vip.test ./client/vip/
wsl -e sh -c 'sudo ZWAN_TUN_TEST=1 /tmp/vip.test -test.run RealTun -test.v'
```

**관리자 권한 게이트 테스트** (엔진·서버 서비스를 멈춘 상태에서):

```powershell
$env:ZWAN_NRPT_TEST=1;     go test .\client\nrpt\ -run RealPolicy -v
$env:ZWAN_FIREWALL_TEST=1; go test .\shared\firewall\ -run RealRule -v
```

둘 다 `t.Cleanup` 이 실패하든 말든 머신 상태를 되돌린다.

### 2.2 아직 안 해본 것

1. **이름으로 서비스 접속 + MTU** — §0.1 의 1번. 남은 가장 큰 구멍.
2. **직접 모드(릴레이 없이) 2대** — `client/wgbind` 직접 경로는 여전히 미검증.
   **GUI 로는 불가능하다**: `manager.Connect` 가 `engine.Config.Endpoint` 를 안 채워 항상
   `127.0.0.1:<포트>` 를 광고한다. CLI 로만 된다 —
   `zwan-agent --up --endpoint <상대가 닿는 host:port> --wg-port 51820` (양쪽 UDP 51820 인바운드 허용).
   M3 가 STUN 으로 이걸 제대로 푼다.
3. **설치본 실측 일부** — 재부팅 후 자동 복귀, 언인스톨 시 서비스 2개 제거 + 드라이버 잔존.
4. **ACME 실발급** — 도메인 A 레코드 → `--addr 0.0.0.0:443 --domain <도메인>` → 핀 없이 CA 검증으로 접속.
   컨트롤이 443 이 아니면 **HTTP-01 이라 TCP 80 이 호스트까지 닿아야** 한다.
   Host 탭의 "ACME 인증서" 는 **설정된 모드**일 뿐 발급 여부가 아니다(autocert 는 첫 핸드셰이크 때 받아온다).
5. **자동 업데이트 왕복** — v0.3.3 에서 고친 뒤 끝까지 안 봤다.

---

## 3. 로드맵 (우선순위 순)

1. **M3 NAT 트래버설** ← **다음 차례** — STUN + UDP hole punching + 경로 선택(직접 우선/릴레이 폴백).
   `client/wgbind` 가 연결 지점. 릴레이 대역폭이 걸린 문제.
2. **IPv6 전송** — `wgbind` 가 IPv4-only. 노드 간 IPv6 직접 경로(문서 20·21).
   참고: `client/vip` 도 IPv4 만 변환한다. 어댑터에 IPv6 가 붙으면 그 트래픽은 드롭된다.

---

## 4. 실사용에서 터진 것들 (2026-08-25~26, 전부 수정·배포 완료)

한 줄로 요약하면 **"성공했다고 말하면서 실패하는" 경로가 계속 나왔다.** 공통점은 설정값이 아니라
**관측 불가능성**이다 — 서버는 바인딩돼 있고 UI 는 "연결됨"인데 트래픽만 없다.

| 증상 | 진짜 원인 | 릴리스 |
|---|---|---|
| Host 탭 검은 화면 | `Domains` 가 `omitempty` 없어 `null` 로 나가고, 스프레드가 기본값 `[]` 를 덮어 렌더 throw → **트리 전체 언마운트** | v0.2.1 |
| 핀 불일치 | 공개 주소에 포트를 안 쓰면 가입 주소에서 빠져 클라가 443 으로 붙음 → 남의 서버 지문 | v0.3.1 |
| 클라 타임아웃 | SYSTEM 서비스는 방화벽 허용 팝업이 **뜰 수 없어** 인바운드가 조용히 드롭 | v0.3.2 |
| 가입은 되는데 통신 안 됨 | 릴레이 주소로 **리슨 주소** `0.0.0.0:3478` 을 광고 | v0.3.2 |
| 업데이트해도 그대로 | 자동 업데이트는 `/S` 무인 설치 → `Section /o "Server"` 가 기본 미선택이라 **호스트에서 서버가 통째로 건너뛰어짐**. 게다가 실행 중 서비스가 자기 바이너리를 잠그고, `install` 은 이미 있으면 Fatal | v0.3.3 |
| 이름으로 못 찾음 | GUI 는 기기 이름이 비면 그대로 등록 → hostname 없음 → `updateDNS` 가 건너뜀 (CLI 만 `os.Hostname()` 대체를 갖고 있었다) | v0.3.4 |

**남길 교훈**

- **리슨 주소(`0.0.0.0`, 와일드카드)가 바깥으로 광고되는 자리는 전부 의심할 것.** `JoinURL` 과 릴레이가 같은 실수였다.
- **"(선택)" 입력란은 기본값이 있다는 뜻**이어야 한다. 없으면 조용히 망가진다.
- **에러 메시지가 진범을 가리키지 않을 수 있다.** "핀 불일치"의 실제 원인은 포트 누락이었다.
- **버전 스큐**: 새 GUI + 옛 서비스에서 모르는 JSON 키는 **조용히 사라진다**(`publish` 가 그랬다).
  둘은 같이 설치되므로 업그레이드 중에만 생기지만, 디버깅할 때 먼저 의심할 것.
- **`go test ./...` 는 실제 서비스를 건드릴 수 있다.** `server/ipc` 테스트가 파이프 이름(고정 전역)을
  못 잡으면 진짜 서비스로 요청이 가서 네트워크를 켜고 껐다. 지금은 감지해서 스킵한다.

---

## 5. 제품/배포 하드닝

- **TLS 키 파일 ACL(Windows)** — 자체서명 키가 `%ProgramData%\zwan\tls\server.key`. `0600` 은 Windows 에서
  무의미하므로 같은 머신의 다른 로컬 사용자가 읽을 수 있다. `client/ipc` 의 SDDL 방식처럼 디렉터리 ACL 을
  SYSTEM+Administrators 로 제한할 것. 리눅스 서버는 `/var/lib/zwan/tls` 0700 이라 해당 없음.
  같은 이유로 `server.json`(가입 토큰)·`networks.json`(가입 토큰들)도 함께 볼 것.
- **코드 서명** — 미서명이라 SmartScreen 경고. Authenticode 인증서 필요.
- **CI (GitHub Actions)** — 태그 push 시 `scripts/build-release.sh` 자동 실행(win runner + NSIS + wails). 지금은 로컬 수동.
- **wintun.dll 벤더링** — 지금 로컬 `bin/wintun.dll`. 릴리스는 wintun.net 공식 dll 을 CI 에서 받아 번들.
- **서버 배포 편의** — systemd unit 예시(`Restart=always`, `--auto-update`), Docker 이미지.
- **arm64 클라이언트 / WebView2 부트스트랩** — 설치본은 amd64 전용이고 WebView2 런타임을 가정한다.

---

## 6. 코드 정리 / 남은 숙제

- **NRPT — 별칭이 실제 도메인을 가리면 그 도메인이 통째로 우리에게 온다.** 별칭을 `com` 으로 잡으면
  `*.com` 이 전부 로컬 리졸버로 오고, 존에 없는 이름은 NXDOMAIN 이 되어 인터넷이 끊긴 것처럼 보인다.
  `profile.ValidAlias` 는 문법만 본다. 공용 서픽스를 막을지, 경고만 띄울지 결정 필요.
- **NRPT/리졸버 상태가 GUI 에 안 뜬다.** 규칙이 붙었는지는 로그에만 있다. **리졸버가 53 을 못 잡은
  경우도 안 보인다** — 이때는 규칙도 안 붙어 이름이 통째로 안 풀리는데 화면상 "연결됨"이다.
  `manager.Status` 에 필드 하나 얹으면 네트워크 카드에서 바로 보인다.
- **NRPT — 강제 종료하면 규칙이 남는다.** 다음 기동의 첫 대조에서 지워지고 서비스 제거 시에도
  `nrpt.Purge()` 가 걷어내지만, 그 사이에는 `*.<별칭>` 이 죽은 리졸버를 가리킨다. 일반 이름엔 영향 없다.
- **ACL**: 그룹/규칙을 바꿔도 **이미 붙은 터널은 다음 갱신(3초)까지 살아 있다.** 즉시 끊으려면 별도 무효화 경로 필요.
  방향성 ACL("A→B 는 되고 B→A 는 안 됨")은 피어 가시성 레벨에선 불가능하다(WireGuard 는 양쪽이 서로의 키를 가져야 한다).
  지금은 피어는 대칭, 방향성은 서비스 단위로만 표현한다.
- **B4**: "모든 포트"가 아니라 **게시한 포트**로 도달한다. 전용 주소가 있으니 원래 포트로 게시하면 되고
  그게 요구("포트 정보 없이 이름만")를 충족한다. 문자 그대로의 전-포트가 필요해지면 gVisor netstack 이 후속.
- **TLS**: 토큰에 핀을 묶은 단일 초대 코드, 핀 회전 절차.
- **`client/ipc` 파이프 왕복 테스트 없음** — `server/ipc` 에는 있다(`ipc_windows_test.go`). 같은 형태로 복제.
  단 그 테스트는 **서비스가 돌고 있으면 스킵**한다(§4 교훈).
- **`client/engine` Start/Stop 미검증** — 어댑터가 필요해서 유닛 테스트로 못 덮는다. republish·DNS 매핑·이름 정규화만 덮여 있다.
- **로깅** — 서비스 로그가 콘솔로만 나간다. Windows 이벤트로그 또는 `%ProgramData%\zwan\logs` 로.

---

## 7. 참고 (아키텍처 결정)

- 엔진 = SYSTEM 서비스(`zwanEngine`), 컨트롤 서버 = SYSTEM 서비스(`zwanServer`), GUI/트레이 = 사용자 권한.
  각각 named pipe 로 통신(SDDL: SYSTEM/Admins full, Interactive RW).
- 클라 상태: `%ProgramData%\zwan\` — `device_id`, `networks.json`, `tls/`, `server.json`.
  `ZWAN_STATE_DIR` 로 통째로 옮길 수 있다(테스트·포터블·나란히 띄우기용).
- 주소 공간 3층: **오버레이**(서버가 배정, 터널 안에서만) / **로컬**(기기별, 네트워크마다 `100.112.0.0/12` 에서 한 조각) /
  **서비스**(오버레이 대역의 위쪽 절반, 기기는 아래쪽 절반).
- 이름은 서버 서픽스가 아니라 **기기가 정한 별칭**으로 스코프한다(문서 47.1). 두 서버가 같은 서픽스를 쓸 수 있기 때문.
- **NRPT 규칙은 프로세스가 아니라 머신에 붙는다.** 그래서 `client/nrpt` 는 자기가 기억하는 것이 아니라
  **실제로 설치된 표**와 대조한다. 우리 규칙은 `Comment = "zwan-split-dns"` 로 식별하고, 그 표식이 없는
  규칙(도메인 정책 등)은 절대 건드리지 않는다. 대상은 네트워크 목록이 아니라 `resolver.Suffixes()` —
  답할 수 없는 동안 규칙이 있으면 그 이름은 폴백하지 않고 실패하기 때문.
- **방화벽 규칙도 머신에 붙는다.** 포트가 아니라 **실행 파일**을 지정한다(포트는 전부 설정값이고 터널은
  네트워크마다 새로 잡는다). `profile=any` — 호스팅 기계는 인터넷에서 접근되는데 Windows 는 그 연결을
  보통 Public 으로 분류한다.
- **게시 서비스 목록은 `networks.json` 에 있다.** 서버 레지스트리는 메모리라, 양쪽 어느 쪽이 재시작해도
  이 파일이 서비스를 되살린다. 그래서 게시 편집은 해당 네트워크를 재접속시킨다(가입 시점에 정해지므로).
- **UDP 53 은 "누가 듣고 있나"로 판단하면 안 된다.** WSL2 나 Hyper-V 기본 스위치가 있으면 ICS
  (`SharedAccess`, svchost)가 **`0.0.0.0:53`** 을 잡는다. 그런데도 `127.0.0.1:53` 바인딩은 **그대로
  통과한다** — 와일드카드 소켓이 `SO_EXCLUSIVEADDRUSE` 가 아니면 Windows 가 특정 주소를 허용하기 때문.
  실측 확인했다. 그래서 검사는 엔진이 하는 그대로 **직접 바인딩해 보는 것**이어야 한다.
- 브랜드명은 `shared.ProductName`(현재 "MyWAN") 한 곳. 기술 식별자는 `zwan` 고정.
- 라이선스 위생: 퍼미시브만(MIT/BSD/Apache/ISC). Wintun 은 프리빌트 dll 동적로드(Tailscale 방식).
- 릴리스: `scripts/build-release.sh <버전>` → `dist/`, `gh release create v<버전> ...`.
  스크립트는 **이 작업 트리에서 실행 중인 앱**이 있으면 거부한다(설치본은 상관없다).
