# MyWAN 구현 계획서 (오픈소스 셀프호스팅 오버레이 네트워크)

> 근거 문서: `MyWAN_가상네트워크_아이디어_정리.md` (1~31 기존 개념, 32~46 셀프호스팅·다중망 확장, 47 요구사항 갱신)
> 목표 환경: **Windows**(서버는 Linux VPS 겸용). 산출물: **셀프호스팅 서버 1종 + Windows 클라이언트 1종**.
> 스택 확정: **Go + Wails v2(React/TS/shadcn) + wireguard-go/Wintun / 라이선스 Apache-2.0**.

---

## 0. 목표와 범위

**한 문장 목표:** 누구나 자기 서버를 호스팅해(공인 IP 필요) 자기만의 Private 오버레이 네트워크를
만들고, 클라이언트는 여러 네트워크에 동시 가입해 이름만으로 서비스에 접속한다.

**이번 범위(In scope)**
- 셀프호스팅 Control Plane 서버(인증/멤버십, IPAM, 내부 DNS, 서비스 레지스트리, ACL, STUN, Relay)
- Windows 클라이언트 Agent(가상 NIC, 암호화 터널, Split DNS, L4 서비스 라우팅, 다중 네트워크)
- **다중 네트워크 동시 가입**(핵심 차별점, 37~40·47.1절)
- **서버·클라이언트 모두 데스크톱 앱**: GUI 설정 + 트레이 백그라운드 상주(47.2절)
- **도메인 선택제**: 가입 = 서버주소(도메인 또는 IP) + 토큰/비밀번호 + 로컬이름(47.1절)
- 데모: 서버 1 + Windows 클라이언트 2 로 이름 기반 서비스 접속

**이번 범위 밖(후속)**
- 모바일/맥 클라이언트, 웹 관리 대시보드(초기엔 데스크톱 GUI), 전송용 IPv6/ULA, 대규모 SDN,
  SaaS 운영, 서비스 Failover 자동화.

---

## 1. 핵심 설계 결정 (확정)

| # | 항목 | 결정 | 근거 |
|---|------|------|------|
| D1 | 구현 언어 | **Go — 확정** | 오버레이 코어를 검증된 자산으로 시작(**Tailscale=Go로 데몬→트레이GUI→Wintun 전부**). **성능 트레이드오프 수용**: 병목은 WAN 구간이고 암호화는 Go stdlib 어셈블리 최적화, Tailscale이 프로덕션 증명. 단일 바이너리로 Windows+Linux 겸용 |
| D2 | 데이터 평면 | **WireGuard/Noise** (`wireguard-go`+`wintun`) | 검증된 암호. 자체 암호 설계 금지(29·44절) |
| D3 | 컨트롤 API | **HTTPS/JSON + long-poll — 확정** | 443 방화벽 친화, ACME 궁합, 구현 단순. gRPC는 후속 여지 |
| D4 | 서버 저장소 | **SQLite 내장**(modernc.org/sqlite, 순수 Go/BSD) | 단일 바이너리 내장, cgo 불필요 |
| D5 | 네트워크 네임스페이스 | **유일 DNS 서픽스 필수 / 실도메인 선택**(47.1) | 무도메인 시 서버 발급 서픽스 + **클라 로컬 별칭** |
| D6 | 다중 네트워크 주소 | **네트워크당 Wintun 어댑터 + 클라이언트-로컬 VIP**(38절) | OS 라우팅/주소 충돌 원천 차단 |
| D7 | 인증 | **초대 토큰 또는 비밀번호 + 노드 키**(35·47.1) | 토큰 폐기 가능, 노드 키 지속 인증 |
| D8 | TLS 신뢰 | **도메인 시 ACME, 무도메인 시 지문 핀닝**(36·47.1) — 구현 완료 ✅ | 핀은 인증서가 아닌 **SPKI**(공개키) 해시라 인증서 재발급에도 불변. `server/tlsconf` + `shared/certpin` |
| D9 | 데스크톱 셸 | **엔진(서비스) + 프론트엔드(Wails v2) 분리 — 확정** | 트레이=`fyne.io/systray`, GUI=**Wails v2**(WebView2), 서비스=`x/sys/windows/svc`. 상세 §10 |
| D10 | 라이선스 | **Apache-2.0 — 확정**(MIT 대안) | 소스 공개 불필요, 귀속(About 명시)만 요구. 상세 §10 |

---

## 2. 시스템 아키텍처

### 2.1 서버 — `zwan-server` (데스크톱 앱 + 헤드리스 겸용, 47.2)
```
zwan-server
 ├─ 엔진(백그라운드/서비스 · Linux VPS에서는 헤드리스)
 │   ├─ ctrl-api   HTTPS: 등록/멤버십/피어 디렉터리/서비스·ACL 조회(long-poll 푸시)
 │   ├─ auth       토큰/비밀번호 발급·검증, 노드 키 인증
 │   ├─ ipam       네트워크 대역에서 노드 IP 할당(Device UUID 고정)
 │   ├─ dns        서픽스 권위 응답(노드/서비스 레코드)
 │   ├─ registry   서비스 등록/조회(VIP·프로토콜·포트·백엔드)
 │   ├─ acl        그룹/역할 접근 제어
 │   ├─ stun       NAT 판별/엔드포인트 수집(3478/udp)
 │   ├─ relay      P2P 실패 폴백(DERP류)
 │   └─ store      SQLite(modernc.org/sqlite)
 ├─ 트레이        실행/중지, 가입자·릴레이 상태, 로그
 └─ GUI 설정창    네트워크 생성/편집, 토큰·비밀번호, 멤버 승인, 서비스·ACL, 포트/인증서
```

### 2.2 클라이언트 — `zwan-agent` (데스크톱 앱, 47.2)
```
zwan-agent
 ├─ 엔진(Windows Service, SYSTEM)
 │   ├─ profile-mgr    네트워크별 프로파일(다중 가입, 43절)
 │   ├─ tun-mgr        네트워크당 Wintun 어댑터
 │   ├─ wg-engine      wireguard-go 다중 세션(네트워크별 키)
 │   ├─ nat-traversal  STUN+hole punching+경로 선택+relay 폴백(8·41절)
 │   ├─ router         Service Router: 로컬VIP:포트 → 오버레이 백엔드 L4(TCP/UDP)
 │   ├─ vip-alloc      클라이언트-로컬 VIP 배정/매핑(38절)
 │   ├─ resolver       127.0.0.1:53, 서픽스→네트워크 판별→로컬VIP(39·47.1)
 │   └─ os-net         라우트/NRPT/방화벽(Windows API/netsh/PowerShell)
 ├─ 트레이            네트워크 목록/상태, 빠른 연결·해제
 └─ GUI 설정창        가입 대화상자(서버주소+토큰/비밀번호+로컬이름), 네트워크·서비스 목록, 경로 상태
```

### 2.3 공용 — `zwan-shared`
```
프로토콜 타입 · 와이어 포맷 · 키/암호 유틸 · 주소/CIDR 유틸 · 버전 협상 · 로컬 IPC(엔진↔GUI)
```

### 2.4 제어/데이터 평면 분리(2절 유지)
Control Plane = 제어 정보만. Data Plane = 클라↔클라 P2P, 실패 시 서버 Relay.

---

## 3. 저장소 레이아웃
```
vlan-controller/            (go module: github.com/Zeliper/zwan)
 ├─ cmd/
 │   ├─ zwan-server/       서버 main(엔진+트레이+GUI 부트스트랩)
 │   └─ zwan-agent/        클라이언트 main(+ Windows 서비스 등록)
 ├─ server/                 ctrl-api, auth, ipam, dns, registry, acl, stun, relay, store
 ├─ client/                 profile, tun, wg, nat, router, vip, resolver, osnet
 ├─ shared/                 proto, crypto, netutil, ipc
 ├─ frontend/               Wails 프론트엔드(React+TS+Vite+Tailwind+shadcn) — 서버/클라 화면 공용
 ├─ docs/                   호스팅 가이드, 프로토콜 스펙, 아키텍처
 ├─ installer/              MSI/winget, wintun.dll 재배포, WebView2 부트스트랩
 ├─ test/                   통합/e2e 하니스
 ├─ go.mod / wails.json
 └─ README / LICENSE / NOTICE / THIRD_PARTY_NOTICES / CONTRIBUTING
```

---

## 4. 데이터 모델 개요 (서버 저장소)
```
Network   { id, name, dns_suffix, domain?(선택), overlay_cidr, cert_mode, created_at }
Member    { id, network_id, uuid, node_pubkey, assigned_ip, role, groups[], status }
AuthCred  { id, network_id, kind(token|password), hash, reusable, expires_at, role, used_by }
Service   { id, network_id, name, vip(논리), proto, vport, node_id, backend_addr }
ACL       { id, network_id, src, dst, action }
Endpoint  { member_id, ip:port 후보들, last_seen }
```

---

## 5. 마일스톤 (증분 검증형)

### M0 — 스캐폴드 & 공용 프로토콜  ← 착수 지점
- go mod(`github.com/Zeliper/zwan`), 3-바이너리 골격, 프로토콜 타입, 키 생성, 설정 포맷, 엔진↔GUI IPC 뼈대, LICENSE/NOTICE. 브랜드명은 `shared.ProductName` 한 곳에서 관리(언제든 변경).
- **완료:** `go build ./...`(Windows) 통과, 서버 엔진 헬스체크 응답.

### M1 — 단일 네트워크 · 2 클라이언트 오버레이(릴레이 경유)
- **M1a (완료 ✅)**: 서버 등록/멤버십·정적 IPAM·피어 디렉터리 + 노드 키(X25519, stdlib) + 클라 가입 흐름.
  `go test` 및 2-에이전트 실증(100.64.0.1/.2 고유·안정 IP, /v1/peers, 토큰 검증) 통과.
- **M1b-1 (완료 ✅)**: Wintun 어댑터 생성 + 오버레이 IP 할당(`--tun`). 관리자 권한 실증 통과
  (adapter up, Get-NetIPAddress 인식, 자기 IP 핑 응답, 정상 제거).
- **M1b-2 (완료 ✅)**: wireguard-go 암호 터널 + 엔드포인트 교환 → 두 클라이언트가 상호 **handshake OK**(직접 경로).
  Windows `SIO_UDP_CONNRESET` 문제를 커스텀 conn.Bind(`client/wgbind`)로 해결.
  - 단일 PC 한계: 두 오버레이 IP가 모두 로컬이라 **실제 오버레이 `ping`은 2대(또는 VM/WSL) 필요**.
    핸드셰이크 성립으로 데이터플레인·키·전송을 증명함(wgprobe 2-프로세스 + admin 2-클라이언트).
- **M1b-3 (완료 ✅)**: 서버 **Relay**(공인 IP 경유) — 클라이언트가 WG 패킷을 오버레이IP 프레이밍으로
  릴레이에 보내면 릴레이가 목적지로 포워딩. NAT 뒤 클라이언트끼리 직접 도달 불가여도 연결.
  검증(관리자 불필요): `wgprobe --relay` 2-프로세스 + `go test ./test`(릴레이 경유 handshake).
- **완료(전체):** 클라 2대가 오버레이 IP로 상호 `ping` (실측은 2대 환경에서; 릴레이/직접 경로 모두 handshake 검증됨).

### M2 — 내부 DNS · 서픽스 네임스페이스 · Service VIP · L4 매핑
- **M2a (완료 ✅)**: 서비스 레지스트리(`/v1/services`) + 클라이언트 로컬 DNS 리졸버(miekg/dns, `*.<서픽스>` → 노드/서비스 IP).
  `--publish-name/-port`로 서비스 게시, `--up` 시 리졸버 기동 + 주기적 레코드 갱신. 검증: 서비스 게시/조회 실측 + 리졸버 유닛 테스트(A/NXDOMAIN).
- **M2b (완료 ✅ · 호스팅측 L4)**: 호스팅 노드가 `overlayIP:port`로 받아 `127.0.0.1:backend`로 포워딩하는
  L4 TCP Service Router(`client/l4`). 백엔드는 localhost-only 유지(19절). 유닛 테스트 통과. `--publish-backend-port`로 활성화.
- **M2b-2 (완료 ✅ · 서비스 전용 주소 + UDP)**:
  - **서버가 서비스마다 주소를 준다.** `ipam.Split` 이 네트워크 대역을 반으로 갈라 아래쪽은 기기,
    위쪽은 서비스에 쓴다(기기 주소는 예전 그대로 `.0.1` 부터 시작하므로 기존 네트워크에 변화 없음).
    주소 키는 **서비스 이름**이라, 재게시하거나 다른 노드로 옮겨도 클라이언트가 이미 해석한 주소가 유지된다.
  - **DNS 가 그 주소를 반환**한다. 그래서 이름만으로 충분하다 — 클라이언트는 프로토콜이 원래 쓰는 포트로
    붙고, 사용자가 찾아볼 포트가 없다. 같은 포트의 두 서비스는 주소가 달라 구분된다(11·16절).
  - **호스팅 노드**가 그 주소를 어댑터에 추가하고(`tun.AddServiceIP`) 거기에 리스너를 연다.
    같은 머신의 두 서비스가 같은 포트를 쓸 수 있는 이유가 이것이다.
  - **피어의 AllowedIPs** 에 그 노드가 호스팅하는 서비스 주소가 함께 실린다. 없으면 서비스로 가는 패킷이
    어떤 피어에도 매칭되지 않아 그냥 사라진다.
  - **UDP 프록시**(`client/l4/udp.go`) — 클라이언트마다 백엔드로 가는 소켓을 따로 열어 응답을 되돌린다.
    UDP 에는 종료가 없으므로 유휴 시간으로만 정리한다(90초). 게임 다수가 UDP 라 필수.
  - **접근 제어**는 TCP 와 동일하게 출발지 그룹을 확인한다.
  - **한계**: "모든 포트"가 아니라 **게시한 포트**로 도달한다. 전용 주소가 있으니 서비스를 원래 포트로
    게시하면 되고, 그것이 요구사항("포트 정보 없이 이름만")을 충족한다. 문자 그대로 임의 포트를 받으려면
    유저스페이스 TCP/UDP 스택(gVisor netstack) 또는 상태 기반 NAT 이 필요한데, 이득 대비 비용이 크다.
  - **검증**: `ipam` 분할 테스트, `server/api` 서비스 주소 테스트(고유·재게시 안정·목록 노출),
    `client/l4` UDP 프록시 테스트, `client/engine` 이름→주소 해석 테스트, `client/wg` 다중 AllowedIP 테스트,
    실바이너리로 **같은 포트 25565 서비스 2개 + UDP 서비스 1개**가 각각 다른 주소를 받는 것 확인.
- **M2c (admin)**: NRPT로 시스템 DNS에 `*.<서픽스>` → 로컬 리졸버 연결 + 전체 경로 실측(2대).
- **완료:** 서비스를 `이름`만으로 접속(백엔드 127.0.0.1 only). 현재 TCP·호스팅측 완료, 시스템 DNS/실측은 M2c.

### M3 — NAT 트래버설 · 경로 선택 · Relay 폴백
- STUN + UDP hole punching + 경로 우선순위(8절) + 릴레이 폴백.
- **완료:** 서로 다른 NAT 뒤 두 클라가 직접 P2P, 불가 시 자동 릴레이.

### M4 — 다중 네트워크 클라이언트 (핵심 차별점)  (1단계 ✅ / 2단계 남음)
- **1단계 (완료)** — 엔진 N개 동시 구동:
  - `client/manager` 가 네트워크별 엔진을 감독한다. **네트워크당 어댑터·노드 키·UDP 포트**가 분리되고,
    포트는 사용 중인 것을 피해 자동 배정한다. 격리는 이 분리에서 나온다 — 각 엔진은 자기 어댑터에
    자기 피어만 프로그램하고, OS IP 포워딩은 꺼져 있으므로 A↔B 경로가 없다(40절).
  - **리졸버는 하나**다. `127.0.0.1:53` 은 한 번만 점유 가능하므로 네트워크별 리스너가 아니라
    **존**으로 나눴다(`resolver.SetZone/RemoveZone`). 네트워크를 떠나면 그 이름은 즉시 안 풀린다.
  - **로컬 별칭 서픽스**(47.1) — 이름은 서버가 알려주는 서픽스가 아니라 기기가 정한 별칭으로 스코프한다.
    두 서버가 같은 서픽스를 쓸 수 있기 때문이다. `engine.Config.Alias`.
  - `client/profile` 이 가입한 네트워크 목록을 `%ProgramData%\zwan\networks.json` 에 보관하고,
    `AutoConnect` 로 재부팅 후 상태를 재현한다. **연결 실패해도 네트워크는 기억**한다(서버가 잠깐 죽었다고
    어디 속했는지를 잃으면 안 된다).
  - IPC/GUI/트레이가 전부 목록 기반으로 바뀌었다. 트레이는 네트워크별 토글 슬롯.
  - `shared.StateDir` 에 `ZWAN_STATE_DIR` 오버라이드 추가 — 테스트가 머신 상태를 건드리지 않는다.
- **2단계 (완료)** — **클라이언트-로컬 VIP 변환**(38절), `client/vip`:
  - **삽입 지점**: wireguard-go 가 임의의 `tun.Device` 를 받으므로, 변환 래퍼를 어댑터와 wg 사이에 끼웠다.
    위(OS)는 로컬 주소만, 아래(터널)는 실제 오버레이 주소만 본다. 그 사이 어디에도 두 대역을 아는 코드가 없다.
  - **매핑**: 네트워크마다 로컬 풀(`100.112.0.0/12` 기본)에서 `/16` 한 조각. 가능하면 실제 주소의 하위
    비트를 유지해(100.64.0.7 → 100.112.0.7) 로그를 읽기 쉽게 하되, 겹치면 빈 자리를 쓴다.
    **테이블은 자기 네트워크 대역만 매핑**한다 — 그래야 피어가 로컬 공간의 임의 주소인 척할 수 없다.
  - **체크섬**: IPv4 헤더 + TCP/UDP(의사헤더에 주소가 들어간다)를 RFC 1624 증분 갱신.
    UDP 체크섬 0(미계산)은 0으로 유지하고, 갱신 결과가 0이면 0xffff 로 쓴다. 조각(fragment)은 첫 조각만
    L4 헤더를 갖고 있으므로 나머지는 IP 헤더만 고친다.
  - **ICMP 오류 인용부**: 오류가 인용한 원본 헤더도 같이 변환한다. 그게 호스트가 오류를 소켓에 연결하는
    수단이라, 빼먹으면 PMTUD·connection refused 가 조용히 죽는다. 인용 헤더 체크섬과 ICMP 체크섬이
    서로 물려 있어 둘 다 갱신한다.
  - **드롭 방향**: 매핑 없는 주소는 통과시키지 않는다(fail closed). Read 쪽은 wireguard-go 가 버퍼와
    상태를 인덱스로 묶으므로 **길이를 0으로** 만들어 건너뛰게 하고(제거하지 않는다), Write 쪽은 걸러낸다.
  - **경고 처리**: 변환 중인 네트워크는 오버레이 대역이 라우팅 테이블에 올라가지 않으므로 겹침 경고를 내지 않는다.
    `NoTranslate` 로 끄면 예전처럼 경고만 한다.
  - **검증**: 체크섬을 **독립적으로 재계산**해 확인하는 테스트(TCP/UDP/ICMP echo/ICMP 오류/조각/무체크섬 UDP),
    왕복 시 원본 바이트 복원, 매핑 없는 주소 거부, 테이블 할당(하위비트 유지·충돌 대체·네트워크/브로드캐스트 제외·고갈),
    가짜 TUN 으로 Read/Write 방향 검증.
- **검증(1단계):** `client/manager`·`client/profile`·`client/resolver` 단위 테스트 + 실제 서비스 파이프 왕복
  (별칭 정규화, 실패해도 기억, 잘못된 별칭 거부, disconnect 는 유지·forget 은 삭제, 재기동 후 복원).

### M5 — 데스크톱 앱(서비스+트레이) · 서버 GUI · 설치파일 · 자동 업데이트 · 릴리스  (✅ 대부분 완료)
- **아키텍처**: 엔진을 **SYSTEM Windows 서비스**(`cmd/zwan-service`)로, 트레이/GUI는 사용자 권한 → **named-pipe IPC**(`client/ipc`).
  재사용 엔진 `client/engine`(가입·어댑터·터널·라우팅·DNS·L4). ✅ 빌드/기동 검증.
- **클라 GUI(Wails v2)**: 트레이 상주(창 닫으면 트레이로) + Connect/Disconnect + 상태(피어 handshake 표시) + **시스템 다크/라이트(shadcn)**. ✅
- **서버 GUI**: 같은 앱 Join/Host 탭 — 인프로세스 호스팅(`server/host`), 토큰 생성/공유, 멤버·서비스. ✅
- **설치파일(NSIS)** `zwan-setup.exe`: GUI+서비스+**Wintun 가상 WAN 드라이버 설치**(`driver-install`)+서비스 등록/시작+로그인 시작+언인스톨러. ✅ 빌드
- **자동 업데이트**(`client/update`): GitHub 릴리스 확인 → 최신 설치본 조용히(/S) 설치(재설치 아닌 자동 업데이트). ✅ 공개 릴리스와 연동
- **릴리스**: repo **public** 전환 + **v0.1.0** 게시(클라 설치본 + 서버 win/linux + SHA256SUMS). ✅
- 남음(후속): ACL 적용, 코드 서명, **실사용 실측**(설치본 관리자 실행 → 드라이버·서비스 설치 → GUI Connect 터널).
- **완료(목표):** 설치본 실행 → GUI로 호스팅/가입 → 자동 업데이트까지 자립. (실측은 관리자 설치 필요)

### M5b — 컨트롤 플레인 TLS (ACME / 자체서명 핀닝)  (✅ 완료)
- **서버 `server/tlsconf`**: 모드 `auto|off|self|acme`. `auto`는 `--domain`이 있으면 ACME, 없으면 자체서명.
  - **ACME**: `golang.org/x/crypto/acme/autocert`(BSD-3, 신규 모듈 의존성 없음). 인증서 캐시는 상태 디렉터리에.
    컨트롤 API가 :443이면 TLS-ALPN-01로 충분하고, 그 외 포트면 **HTTP-01용 :80 리스너를 자동 기동**한다.
  - **자체서명**: ECDSA P-256 키를 `<상태디렉터리>/tls/server.key`에 **한 번만** 생성해 재사용.
    인증서는 만료 임박·SAN 부족 시에만 재발급하고 키는 그대로라 **핀이 변하지 않는다**.
    SAN = localhost/127.0.0.1/::1 + `--domain` + `--tls-san` + 로컬 인터페이스 주소.
- **핀 형식 `shared/certpin`**: `sha256:<base64(SHA-256(SubjectPublicKeyInfo))>`. 입력은 hex/base64,
  접두사 유무 모두 허용. 클라이언트는 `InsecureSkipVerify` + `VerifyPeerCertificate`로 **체인 대신 핀**을 검증한다
  (IP 접속이라 호스트명 검증이 무의미하므로 의도된 대체).
- **가입 문자열 1개로 배포**: 서버가 `https://host:port#sha256:...` 를 출력한다. `join.SplitPin`이 조각을 분리하고,
  GUI Server 칸에 통째로 붙여넣으면 핀 칸이 자동으로 채워진다.
- **클라이언트**: `join.Client`(서버당 1개, 핀 인지) → `engine.Config.Pin`, `ipc.ConnectArgs.Pin`,
  `zwan-agent --pin`, GUI `Connect(server, pin, ...)`. 스킴 생략 시 **https 기본**.
- **검증**: `go test ./...`(certpin/tlsconf/join/host 신규 테스트 포함) + 실바이너리 종단 검증 —
  핀 일치 가입 성공, 핀 불일치·핀 없음·평문 http 접속은 모두 거부, 서버 재기동 후 핀 동일, `--tls=off` 동작.

### M5c — 컨트롤 API 인증 (노드 토큰)  (✅ 완료)
- 문제: `/v1/peers`·`/v1/services` 가 **무인증 GET** 이었다. API에 닿기만 하면 전체 멤버·서비스 목록이
  노출됐고, 누구든 임의 노드 IP로 서비스를 게시할 수 있었다. ACL 이전에 "누가 부르는가"가 없었다.
- **자격증명 2단 분리**: *가입 토큰*(공유 비밀)은 `/v1/register` 에서만 통한다. 등록 시 서버가
  **기기별 노드 토큰**(256bit 랜덤)을 발급하고, 이후 모든 호출은 `Authorization: Bearer <노드토큰>`.
  재등록하면 이전 토큰은 즉시 무효(`store.Network.byToken` 인덱스 동기화).
- **게시 권한**: 서비스는 **호출자 자기 노드에만** 게시 가능(`node_ip` 생략 시 호출자 IP로 채움,
  타 노드 지정은 403). 이름을 남의 주소로 가리키는 것을 막는다.
- **서버 재시작 복원력**: 저장소가 인메모리라 재시작하면 노드 토큰이 사라진다. `join.Client` 가 401을
  받으면 **같은 노드 키로 1회 재등록** 후 재시도한다(키가 바뀌면 터널이 끊기므로 키 재사용이 핵심).
  재할당된 오버레이 IP가 달라지면 조용히 쓰지 않고 오류로 표면화한다.
- **GUI**: 인프로세스 호스트는 자기 API를 HTTP로 호출하지 않고 `host.Members()/Services()` 로
  저장소를 직접 읽는다(서버는 멤버 자격증명을 갖고 있지 않으므로).
- **검증**: `server/api` 테스트 전면 재작성(무인증·가입토큰 재사용·만료 토큰·타노드 게시 모두 거부) +
  `client/join` 재등록 테스트 + 실바이너리 확인(401/200, 에이전트 가입·게시 정상).

### M5d — 서버도 서비스 + 트레이 + GUI + 설치파일 (47.2절)  (✅ 완료)
- 지금까지 서버는 헤드리스 바이너리뿐이었고, GUI Host 탭은 **인프로세스**라 앱을 닫으면 네트워크가 죽었다.
  47.2절이 요구하는 "서버도 엔진(서비스) + 프론트엔드(GUI/트레이) 분리"를 클라이언트와 같은 구조로 맞췄다.
- **`zwanServer` Windows 서비스** — `zwan-server service install|uninstall|start|stop|run`.
  SCM이 인자 없이 띄우므로 설정은 `%ProgramData%\zwan\server.json`(`server/config`)에서 읽는다.
  `AutoStart` 는 "마지막에 켜 뒀는가"를 뜻하며 Start/Stop 이 직접 기록해 재부팅 후 상태가 재현된다.
- **`server/ipc`** — `\\.\pipe\zwan-server`, `client/ipc` 와 같은 SDDL(SYSTEM/Admins full, Interactive RW).
  ops: `status|start|stop`. 파이프 왕복 테스트 추가(`client/ipc` 는 여전히 테스트 없음).
- **`server/supervisor`** — 설정 영속화 + 호스트 기동/중지를 한 곳에. 서비스와 GUI 폴백이 **같은 코드**를 쓴다.
- **GUI Host 탭** — 서비스가 있으면 IPC로, 없으면 인프로세스로 동작하고 후자는 "앱을 닫으면 멈춘다"고 표시한다.
  폼은 저장된 설정으로 시드되고, 시작 시 그대로 서비스에 저장된다.
- **트레이** — 두 역할을 모두 노출: Disconnect(클라) / Stop hosting(서버), 상태에 따라 활성/비활성,
  툴팁에 가입·호스팅 상태 표시.
- **설치파일** — 컴포넌트 선택형으로 전환: `Client`(엔진 서비스 + Wintun) / `Server`(컨트롤 서버 서비스).
  서버만 선택하면 터널 드라이버 없이 서버 박스로 설치된다. 언인스톨은 두 서비스 모두 정리.
- **검증**: `service run` 콘솔 모드 + 파이프로 status/start/stop 왕복, 클라 실접속, 재기동 시 auto-start 복귀,
  중지 시 `autoStart:false` 영속, NSIS 빌드(3 섹션) 통과.

### M5e — 그룹 ACL (18·40절)  (✅ 완료)
- **그룹의 출처**: 어떤 가입 토큰으로 등록했는지가 그룹을 정한다. 클라이언트가 보내는 값은 관여하지 않는다.
  `--token` = `default` 그룹, `--join-token <그룹>=<토큰>` 반복으로 그룹 추가.
  한 토큰이 두 그룹에 걸리면 **기동 거부**(`acl.BuildJoinTokens`) — 애매한 토큰은 정책 구멍이다.
- **정책**: `shared/acl` 의 `Policy{Rules []Rule{Src,Dst []string}}`. `*` 와일드카드, 그룹명 대소문자 무시.
  **규칙 0개 = 전체 허용**(기존 동작 보존), **1개라도 있으면 default-deny**.
  CLI 는 `--acl "dev->nas"` 축약형(반복) + `--acl-file` JSON.
- **강제는 필터링이 아니라 누락**:
  - `/v1/peers` — 호출자 그룹과 연결 허용된 멤버만(자기 자신은 항상 포함). 피어의 공개키를 못 받으면
    **터널 자체가 성립하지 않으므로** 이게 실질적 강제다.
  - 피어 가시성은 **대칭**(`Connected` = 양방향 중 하나라도 허용). WireGuard 는 양쪽이 서로의 키를
    가져야 하므로 단방향 피어 가시성은 성립할 수 없다. 방향성은 서비스에서만 의미를 가진다.
  - `/v1/services` — 호스팅 노드가 보이는 서비스만, 추가로 `AllowGroups` 통과분만.
- **서비스 단위 허용 그룹**(`proto.Service.AllowGroups`, 에이전트 `--publish-allow dev,ops`):
  **두 곳에서 검사**한다. 서버가 목록에서 숨기고, **호스팅 노드의 `client/l4` 프록시가 출발지 그룹을 확인**해
  거부한다. 후자가 없으면 "주소·포트를 알면 붙을 수 있다"가 되어 18절의 약속이 깨진다.
  `l4.AccessPolicy` 는 갱신 루프가 통째로 교체하는 라이브 뷰라, 멤버십·허용목록 변경이 프록시 재시작 없이
  다음 연결부터 적용된다. **기록이 없는 출발지는 거부**(fail closed).
- **GUI**: Host 탭에서 그룹·토큰 추가/생성과 규칙 편집(한 줄에 하나). 규칙 파싱은 프론트가 아니라
  `App.ParseACL` 로 **Go 파서 한 곳**만 쓴다(표시와 강제가 어긋나지 않게). 멤버·서비스 목록에 그룹 배지.
- **검증**: `shared/acl` 단위 테스트, `server/api` 그룹·가시성·허용목록 테스트, `client/l4` 출발지 필터 테스트,
  실바이너리 3그룹 시나리오(dev/guest 서로 안 보임, nas 만 공유, dev 전용 서비스는 guest 에게 미노출).

### M6 (후속, 선택)
- 전송용 IPv6/ULA, DNS SRV, 서비스 Failover, 웹 대시보드, 성능 튜닝.

> GUI/트레이는 M2에서 최소(상주+상태) → M5에서 완성(설정·가입·관리) 점진 도입.

---

## 6. 테스트 토폴로지
```
[개발 루프백]  단일 Windows: 서버=localhost + 클라 인스턴스 2개(어댑터/포트 분리).
[통합]        Go 하니스: 전송 인메모리 대체, IPAM·VIP·DNS·라우팅·격리 검증.
[E2E]         VPS(공인IP) 서버 1 + Windows VM 2대(다른 NAT). 무도메인/도메인 경로 둘 다.
```

---

## 7. 리스크 & 완화
| 리스크 | 영향 | 완화 |
|--------|------|------|
| Wintun 드라이버 서명/재배포 | 설치 실패 | 배포 wintun.dll(Tailscale 패턴: 프리빌트 dll 동적로드), 설치관리자 처리 |
| Symmetric NAT/UDP 차단 | P2P 실패 | 항상 서버 Relay 폴백(공인 IP), 경로 선택(8절) |
| OS 라우팅/주소 충돌(다중망) | 오작동 | 어댑터 분리 + 로컬 VIP + 로컬 별칭 서픽스(38·47.1) |
| 무도메인 서픽스 충돌 | 해석 실패 | 클라 로컬 별칭 강제, 서버 발급 서픽스 랜덤화(47.1) |
| GUI+엔진 권한 경계 | 서비스 SYSTEM/GUI 유저 | 로컬 IPC 인증(named pipe ACL), 최소 권한 |
| **카피레프트 오염** | 소스 공개 강제 | GPL/AGPL/LGPL 의존 금지, 특히 wireguard-windows(GPL) 코드 미차용 — §10 |
| 자체 암호 유혹 | 취약점 | WireGuard/Noise 재사용 고정 |

---

## 8. 남은 결정 (경미)
1. **DERP 재구현 범위**: Tailscale DERP(BSD) 재사용 vs 최소 자체 릴레이. → M1은 최소 자체 릴레이로 시작 권장.

> 해결됨: 언어(Go), 성능(수용), 컨트롤채널(long-poll), 라이선스(Apache-2.0), GUI(Wails+shadcn+시스템테마),
> 도메인(선택제), module `github.com/Zeliper/zwan`, repo `zwan`(private), git init, 브랜드명 상수화(`shared.ProductName`).

---

## 9. 툴체인 상태 & 착수 체크리스트 (M0)
```
설치됨:  Go 1.26.7 / Node v24.12 / npm 11.6 / git 2.52 / gcc(MSYS2 15.2) / gh(Zeliper) / winget
남음:    Wails CLI → GUI 단계에서:  go install github.com/wailsapp/wails/v2/cmd/wails@latest
```
- [ ] Go 설치(위 명령), Wails CLI 설치
- [ ] `go mod init github.com/Zeliper/zwan`, 3-바이너리 골격, `go build ./...` 통과
- [ ] `shared`: 프로토콜 타입 + 키쌍 생성 + CIDR 유틸 + 엔진↔GUI IPC 뼈대
- [ ] `server`: 헬스체크 + 최소 등록 API + SQLite 스키마
- [ ] `client`: 설정 로드 + Wintun 어댑터 1개 생성 PoC(관리자 권한)
- [ ] `frontend`: Wails init + Tailwind + shadcn + 시스템 테마 토글 PoC
- [ ] LICENSE(Apache-2.0) + NOTICE + THIRD_PARTY_NOTICES 초안
- [ ] 개발 루프백 테스트 하니스 초안

---

## 10. 라이선스 & 프론트엔드 확정

### 10.1 라이선스 = Apache-2.0 (소스 공개 불필요, 귀속만)
- **Apache-2.0**: 내 소스 공개 의무 없음. 의무는 (1) 라이선스/저작권 고지 보존, (2) 변경 파일 표시 정도.
  추가로 **명시적 특허 허여**가 있어 네트워크/암호 프로젝트에 안전. (더 단순하게 원하면 MIT도 동일하게 소스 공개 불필요)
- **귀속 방식**: 앱 About 화면에 사용 라이브러리·라이선스 표기 + 루트 `THIRD_PARTY_NOTICES` 파일. (사용자 허용 범위 내)

### 10.2 라이선스 위생 (중요 — 카피레프트 회피)
```
허용(퍼미시브)만 사용:  MIT / BSD / Apache-2.0 / ISC / Public-Domain
금지:                   GPL / AGPL / LGPL (소스 공개·강한 결합 강제 가능)

핵심 의존성 라이선스
  wireguard-go        MIT           ✓  (터널 엔진으로 사용)
  Wintun(프리빌트 dll) 재배포 허용    ⚠  드라이버 '소스'는 GPLv2이나,
                                       서명된 프리빌트 wintun.dll을 C API로 동적로드하는 방식은
                                       Tailscale(BSD-3)이 동일하게 사용 → 앱은 GPL 파생물 아님.
                                       (빌드 시 Wintun 재배포 약관 최종 확인 필요)
  wireguard-windows   GPLv2         ✗  코드 '차용 금지'(참고만, 재구현)
  modernc.org/sqlite  BSD / SQLite  ✓  (SQLite 자체 Public-Domain)
  Wails v2            MIT           ✓  ; WebView2 런타임 = MS 재배포 허용(프로프라이어터리 런타임)
  React/Vite/Tailwind/Radix/shadcn  MIT ✓
  fyne.io/systray     Apache/MIT    ✓ ;  miekg/dns  BSD  ✓
```

### 10.3 프론트엔드(Wails v2) 스택 확정
```
Wails v2 (Go 백엔드 ↔ WebView2)
  프레임워크 : React + TypeScript + Vite
  스타일     : Tailwind CSS + shadcn/ui (+ Radix) — WebView2=Chromium Edge라 완전 지원
  테마       : 시스템 테마 연동(dark/light)
               - Tailwind darkMode:'class'
               - matchMedia('(prefers-color-scheme: dark)') 감지 + 수동 토글(override)
  대상 화면  : 서버 관리 콘솔 / 클라이언트 가입·상태 콘솔 (컴포넌트 공용)
  런타임 의존: WebView2 Evergreen(대다수 Win10+/11 기본 탑재, 설치관리자에서 보장)
```

---

### 참고
- 설계 근거: `MyWAN_가상네트워크_아이디어_정리.md`
- 유사 오픈소스 계보: Tailscale/Headscale(Go, BSD/Apache), ZeroTier, Netmaker, WireGuard/Wintun
